import 'package:flutter/material.dart';

class messages_screen extends StatelessWidget{
  const messages_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
     // backgroundColor: Colors.blue,

    );
  }

}